// Quantification theory
var domain = 'Eating in a German restaurant'

// first-order function subsumed into quantifier second-order function
everyone(domain) + is(‘German’)

return('Frege' + is(‘German’))

function everyone(domain) = [
‘Frege’,
‘Socrates’,
…
].limitedBy(domain)
